<table:TableViewBase x:Class="Test.Table.TableView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:table="clr-namespace:Test.Table"
        Title="Table" Height="500" Width="800">
    <Grid>

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0" Orientation="Horizontal" HorizontalAlignment="Left">
            <Button Content="Add Row" Command="{Binding AddRowCommand}"/>
            <Button Content="Update Row" Command="{Binding UpdateRowCommand}"/>
            <Button Content="Delete Row" Command="{Binding DeleteRowCommand}"/>
        </StackPanel>
        <DataGrid Grid.Row="1" x:Name="grid" AutoGenerateColumns="False" CanUserAddRows="False" ItemsSource="{Binding DataGrid}" Margin="4, 0">
        </DataGrid>
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Ok" Command="{Binding OkCommand}"/>
            <Button Content="Cancel" Command="{Binding CancelCommand}"/>
        </StackPanel>
    </Grid>
</table:TableViewBase>































