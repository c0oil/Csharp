<test:MainWindowBase x:Class="Test.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:test="clr-namespace:Test"
        Title="MainWindow" Height="350" Width="525">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBox Grid.Row="0" Text="{Binding FindTemplate}" Width="100" HorizontalAlignment="Left" />
        <Button Grid.Row="1" Content="Refresh" Command="{Binding RefreshCommand}" Width="100" HorizontalAlignment="Left"/>

        <DataGrid Grid.Row="2" x:Name="grid" ItemsSource="{Binding GridData}" MinWidth="300" HorizontalAlignment="Left">
            
        </DataGrid>
    </Grid>
</test:MainWindowBase>

