<test:MainWindowBase x:Class="Test.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:test="clr-namespace:Test"
        xmlns:smo="clr-namespace:Test.DbConnection.Smo"
        Title="Hello World" Height="200" Width="400" ResizeMode="NoResize">
    <test:MainWindowBase.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
    </test:MainWindowBase.Resources>
    <Grid>
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
            <StackPanel Orientation="Horizontal">
                <Button Content="Connect to Db" Command="{Binding ConnectCommand}" Style="{StaticResource ConnectButton}"/>
                <Button Content="Show Tables" Command="{Binding ShowTablesCommand}" Style="{StaticResource ColoredButton}"/>
            </StackPanel>
            <StackPanel Orientation="Horizontal" Margin="4"
                Visibility="{Binding Path=IsConnecting, Converter={StaticResource BooleanToVisibilityConverter}}">
                <smo:LoadingCircle/>
                <TextBlock Text="Test connection..." />
            </StackPanel>
        </StackPanel>
    </Grid>
</test:MainWindowBase>



